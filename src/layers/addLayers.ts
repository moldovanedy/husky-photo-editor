/**
 * Adds an image as a layer to a project
 * @param image The image to be added to the project
 * @param layerZIndex The zIndex of the layer
 * @param project The project's uuid
 */
export function addImageAsLayer(
    image: HTMLImageElement,
    layerZIndex: number,
    project: string
) {}
